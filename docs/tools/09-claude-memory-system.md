# Memória — caminho de compatibilidade

O conteúdo desta edição foi adaptado para [memória de projeto no Codex](09-codex-memory-system.md).

Este arquivo mantém o caminho antigo para não quebrar referências externas. O fluxo atual não depende da memória automática do Claude.
